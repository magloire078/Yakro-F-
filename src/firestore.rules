
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Autorise toutes les lectures et écritures pour tout utilisateur connecté.
      // ATTENTION : À utiliser uniquement pour le développement.
      allow read, write: if request.auth != null;
    }
  }
}
    